#pragma once
#include <iostream>

/**
 * Player color helper enum
 */
enum class PlayerColor {
    WHITE = 1,
    BLACK = 2
};

/**
 * Piece type helper enum
 */
enum class PieceType {
    PAWN = 1,
    KNIGHT = 2,
    BISHOP = 3,
    ROOK = 4,
    QUEEN = 5,
    KING = 6
};